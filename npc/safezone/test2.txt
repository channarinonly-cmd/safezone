/*

// ===================================================
// NPC: Item Exchanger (Oridecon to Elunium) 
// Version: Time Limit + Anti-Bug PRO + Logging System
// ===================================================
morocc,167,38,4	script	Refine Exchange	457,{

	// --- ตรวจสอบเวลา (ปิดรับแลกวันอาทิตย์ 19.00 น.) ---
	// gettime(4) คือวัน (0=อาทิตย์, 6=เสาร์) | gettime(3) คือชั่วโมง (0-23)
	if (gettime(4) == 0 && gettime(3) >= 19) {
		mes "[Refine Exchange]";
		mes "ขออภัยด้วยครับ ขณะนี้หมดเวลาการรับแลกแล้ว";
		mes "กิจกรรมนี้สิ้นสุดลงเมื่อ ^FF0000วันอาทิตย์ เวลา 19.00 น.^000000";
		mes "ไว้รอโอกาสหน้านะครับ";
		close;
	}

	mes "[Refine Exchange]";
	mes "สวัสดีครับ ยินดีต้อนรับสู่ระบบแลกเปลี่ยนไอเทม";
	mes "ท่านต้องการทำรายการใด?";
	next;

	switch(select("แลกเปลี่ยนไอเทม:อ่านข้อมูลกิจกรรม:ยกเลิก")) {
		case 1:
			goto L_Exchange;
			break;
		case 2:
			mes "[ ข้อมูลการรับแลก ]";
			mes "1. อัตราแลกเปลี่ยน: ^FF0000Ori 5 ชิ้น^000000 ต่อ ^0000FFElu 3 ชิ้น^000000";
			mes "2. แลกได้สูงสุดครั้งละ ^0088006,000 ชุด^000000";
			mes "3. ^FF0000หมดเขตรับแลก:^000000 ทุกวันอาทิตย์ เวลา 19.00 น.";
			mes "--------------------------------";
			mes "ระวัง! กรุณาเช็คน้ำหนักตัวให้เรียบร้อยก่อนแลก";
			next;
			goto L_Exchange;
			break;
		case 3:
			close;
			break;
	}

L_Exchange:
	mes "[Refine Exchange]";
	mes "ท่านต้องการแลกเปลี่ยนทั้งหมดกี่ ^008800ชุด^000000 ครับ?";
	mes "(พิมพ์ 0 หรือเลขติดลบเพื่อยกเลิก)";
	
	input .@amount;

	// [ป้องกันบัค #1] กันเลข 0 หรือติดลบ
	if (.@amount <= 0) {
		mes "[Refine Exchange]";
		mes "ยกเลิกรายการเรียบร้อยครับ";
		close;
	}

	// [ป้องกันบัค #2] กันเลขล้น (Overflow) จำกัด 6000 ชุด
	if (.@amount > 6000) {
		mes "[Refine Exchange]";
		mes "จำนวนเยอะเกินไปครับ กรุณาแลกไม่เกิน 6,000 ชุด";
		close;
	}

	.@req_item = .@amount * 5; 
	.@get_item = .@amount * 3; 

	// [ป้องกันบัค #3] เช็คจำนวนของในตัว
	if (countitem(984) < .@req_item) {
		mes "[Refine Exchange]";
		mes "ท่านมี ^FF0000Oridecon^000000 ไม่พอครับ";
		mes "ต้องใช้ทั้งหมด ^FF0000" + .@req_item + " ชิ้น^000000";
		close;
	}

	// [ป้องกันบัค #4] เช็คน้ำหนัก
	if (checkweight(985, .@get_item) == 0) {
		mes "[Refine Exchange]";
		mes "น้ำหนักตัวของท่านมากเกินไป กรุณาเคลียร์ตัวก่อนครับ";
		close;
	}

	// เริ่มกระบวนการแลก
	delitem 984, .@req_item;
	getitem 985, .@get_item;

	// ===================================================
	// [ระบบเก็บ Log] 
	// ===================================================
	// คู่มือการดึงข้อมูล Log สำหรับ Admin กันลืม:
	// 1. ระบบจะเขียนข้อความด้านล่างนี้ลงในไฟล์ log/npc.log ของเซิร์ฟเวอร์
	// 2. ถ้าต้องการเช็คประวัติ ให้เข้า PuTTY (SSH) แล้วพิมพ์คำสั่ง:
	//    grep "Refine Exchange" log/npc.log
	// 3. หรือถ้าต้องการดูเฉพาะไอดีผู้เล่น ให้พิมพ์:
	//    grep "Refine Exchange" log/npc.log | grep "ชื่อผู้เล่น"
	// ===================================================
	logmes "[Refine Exchange] ผู้เล่น: " + strcharinfo(0) + " (CharID: " + getcharid(0) + ") นำ Ori จำนวน " + .@req_item + " ชิ้น แลกเป็น Elu จำนวน " + .@get_item + " ชิ้น";

	specialeffect2 EF_STORM_GUST_HIT;
	mes "[Refine Exchange]";
	mes "แลกเปลี่ยนสำเร็จ! ได้รับ Elunium " + .@get_item + " ชิ้น";
	close;

OnInit:
	// สร้างห้องรอแสดงข้อความบนหัว NPC
	waitingroom "รับแลก Ori เป็น Elu", 0;
	end;
}

// =========================================================================
// สคริปต์ NPC แจกไอเทมพิเศษ (พร้อมป้ายชื่อ)
// ตำแหน่ง: Morocc (morocc 169 94)
// เงื่อนไข: 1 ไอดีรับได้เพียง 1 ครั้ง
// ไอเทมที่แจก: 12210 (3 ชิ้น), 12208 (3 ชิ้น), 14592 (3 ชิ้น)
// =========================================================================

morocc,169,94,4	script	พนักงานแจกของรางวัล#01	4_F_KAFRA1,{
	// ตรวจสอบว่ารับไปแล้วหรือยังด้วยตัวแปรระดับไอดี
	if (#ClaimedReward_01 == 1) {
		mes "[พนักงานแจกของรางวัล]";
		mes "ขออภัยค่ะ ไอดีนี้ได้รับของรางวัลไปเรียบร้อยแล้วนะคะ";
		close;
	}

	mes "[พนักงานแจกของรางวัล]";
	mes "สวัสดีค่ะท่านนักผจญภัย!";
	mes "วันนี้ทางเรามีไอเทมสนับสนุนพิเศษมาแจกให้กับทุกท่านค่ะ";
	next;
	mes "[พนักงานแจกของรางวัล]";
	mes "คุณต้องการรับไอเทมตอนนี้เลยหรือไม่คะ?";
	mes "^FF0000(หมายเหตุ: 1 ไอดีสามารถรับได้เพียง 1 ครั้งเท่านั้น)^000000";

	switch(select("รับเลยครับ/ค่ะ:ยังก่อนดีกว่า")) {
		case 1:
			// ตรวจสอบช่องว่างในช่องเก็บของว่าพอรับไอเทมหรือไม่
			if (checkweight(12210, 3) == 0) { 
				mes "[พนักงานแจกของรางวัล]";
				mes "ดูเหมือนว่าช่องเก็บของหรือน้ำหนักตัวของคุณจะเต็มนะคะ";
				mes "กรุณาจัดของให้เรียบร้อยแล้วกลับมาใหม่ค่ะ";
				close;
			}

			// แจกไอเทมตามที่กำหนด
			getitem 12210, 3;
			getitem 12208, 3;
			getitem 14592, 3;
			
			// บันทึกตัวแปรว่ารับไปแล้ว
			set #ClaimedReward_01, 1;

			mes "[พนักงานแจกของรางวัล]";
			mes "ส่งมอบของรางวัลเรียบร้อยแล้วค่ะ!";
			mes "ขอให้สนุกกับการผจญภัยนะคะ!";
			close;

		case 2:
			mes "[พนักงานแจกของรางวัล]";
			mes "ไม่เป็นไรค่ะ หากคุณพร้อมเมื่อไหร่ก็แวะมาหาฉันได้เสมอนะคะ";
			close;
	}

// สร้างป้ายชื่อบนหัว NPC เมื่อ NPC โหลดขึ้นมา
OnInit:
	waitingroom "แจกของชดเชย", 0;
	end;
}
*/